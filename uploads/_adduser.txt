Add user:

_push():

Checks in Reihenfolge:
createUser = false;
1. Existiert User?:
	--> Wenn JA:
	message(2);
	exit;

	--> Wenn NEIN:
	createUser = true;
	2. Existiert Usercard?:
		--> Wenn JA:
		3. Ist es ein Usercard Type?:
			--> Wenn JA:
			4. Ist die Usercard bereits jemandem zugewiesen?
				--> Wenn JA:
				message(3);
				exit;

				--> Wenn NEIN:
				message(1);

			--> Wenn NEIN:
			message(4);
			exit;
		--> Wenn NEIN:
		createUsercard();

if (createUser == true && Usercard exists) {
	createUser();
}

Message():



Success Möglichkeiten:
User + Usercard werden erstellt.
User wird erstellt. Usercard wird nur zugewiesen.

=>
User wird immer erstellt.
Es muss auf Usercard überprüft werden:
- Existenz:
--> Wenn ja, dann Zuweisung.
--> Wenn nicht dann auf False


Fail Möglichkeiten:
User existiert bereits.
Rfid Code ist keine Usercard.